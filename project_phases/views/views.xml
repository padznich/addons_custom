<openerp>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="project_phases.list">
      <field name="name">project_phases list</field>
      <field name="model">project_phases.project_phases</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="project_phases.action_window">
      <field name="name">project_phases window</field>
      <field name="res_model">project_phases.project_phases</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="project_phases.action_server">
      <field name="name">project_phases server</field>
      <field name="model_id" ref="model_project_phases_project_phases"/>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="project_phases" id="project_phases.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="project_phases.menu_1" parent="project_phases.menu_root"/>
    <menuitem name="Menu 2" id="project_phases.menu_2" parent="project_phases.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="project_phases.menu_1_list" parent="project_phases.menu_1"
              action="project_phases.action_window"/>
    <menuitem name="Server to list" id="project_phases" parent="project_phases.menu_2"
              action="project_phases.action_server"/>
    -->
  </data>
</openerp>